apiVersion: v1
kind: Service
metadata:
  name: carve
  namespace: carve
  labels:
    app: carve
    component: http-receiver
spec:
  selector:
    app: carve
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP 